import React, { component } from 'react';
import Aux from '../../hoc/common/aux';

class Register extends component{
    render(){
        return(
            <Aux>
                <form>
                    
                </form>
            </Aux>
        )                    
    }
}

export default Register;